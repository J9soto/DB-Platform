# Generated by dbre_platform.provisioning.k3s.build_cluster_manifest -- do not hand-edit.
# Regenerate: `make k8s-reference`. This is exactly what `dbre request provision --mode k3s` applies.
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: orders-api
  namespace: dbre
  labels:
    app.kubernetes.io/name: orders-api
    app.kubernetes.io/managed-by: dbre-platform
    app.kubernetes.io/part-of: dbre-platform
    dbre.platform/environment: prod
  annotations:
    dbre.platform/tag-application: orders-api
    dbre.platform/tag-cost_center: CC-4471
    dbre.platform/tag-data_classification: confidential
    dbre.platform/tag-environment: prod
    dbre.platform/tag-managed_by: dbre-platform
    dbre.platform/tag-owner: payments-team
    dbre.platform/tag-provisioned_by: dbre-platform
    dbre.platform/tag-team-slack-channel: '#payments-oncall'
    dbre.platform/tag-terraform_managed: 'true'
spec:
  instances: 3
  imageName: ghcr.io/cloudnative-pg/postgresql:16
  primaryUpdateStrategy: unsupervised
  enableSuperuserAccess: true
  storage:
    size: 100Gi
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: '2'
      memory: 4Gi
  monitoring:
    enablePodMonitor: true
  postgresql:
    parameters:
      log_connections: 'on'
      log_disconnections: 'on'
      log_line_prefix: '%m [%p] %q%u@%d '
      log_min_duration_statement: '500'
      max_connections: '220'
    shared_preload_libraries:
    - auto_explain
    - pg_stat_statements
    - pgaudit
  affinity:
    enablePodAntiAffinity: true
    topologyKey: kubernetes.io/hostname
