# Generated by dbre_platform.provisioning.k3s.build_cluster_manifest -- do not hand-edit.
# Regenerate: `make k8s-reference`. This is exactly what `dbre request provision --mode k3s` applies.
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: catalog-api
  namespace: dbre
  labels:
    app.kubernetes.io/name: catalog-api
    app.kubernetes.io/managed-by: dbre-platform
    app.kubernetes.io/part-of: dbre-platform
    dbre.platform/environment: dev
  annotations:
    dbre.platform/tag-application: catalog-api
    dbre.platform/tag-cost_center: CC-1001
    dbre.platform/tag-data_classification: internal
    dbre.platform/tag-environment: dev
    dbre.platform/tag-managed_by: dbre-platform
    dbre.platform/tag-owner: catalog-team
    dbre.platform/tag-provisioned_by: dbre-platform
    dbre.platform/tag-terraform_managed: 'true'
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:16
  primaryUpdateStrategy: unsupervised
  enableSuperuserAccess: true
  bootstrap:
    initdb:
      database: catalog_api
      owner: catalog_api
  storage:
    size: 20Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: '1'
      memory: 512Mi
  monitoring:
    enablePodMonitor: false
  postgresql:
    parameters:
      log_connections: 'off'
      log_disconnections: 'off'
      log_line_prefix: '%m [%p] %q%u@%d '
      log_min_duration_statement: '1000'
      max_connections: '120'
    shared_preload_libraries:
    - pg_stat_statements
